import React from 'react';
import Svg, { Path } from 'react-native-svg';

const ShortLine = (props) => (
  <Svg width={119} height={6} viewBox="0 0 119 6" fill="none" {...props}>
    <Path
      d="M3 3H116.004"
      stroke="#ECF87F"
      strokeWidth={5}
      strokeLinecap="round"
    />
  </Svg>
);

export default ShortLine;
