# ToDo-App

Instructions to run:

    npm install 
    
    cd ios
    pod install
     
    cd..
    npx react-native start

    new terminal:
    
    npx react-native run-ios --simulator="iPhone 16"
